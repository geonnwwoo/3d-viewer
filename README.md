# 3d-viewer

Use the java.awt.Graphics:
```
import java.awt.Color;
import java.awt.Graphics;
import javax.swing.JFrame;
import javax.swing.JPanel;
```
```
```
for graphics.

Main classes for rendering should be stored in the src folder. Classes such as vector, camera, calculation of positions, etc.

The things we are rendering (like a square or something) should be stored in the examples folder.
